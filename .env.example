# Square — https://developer.squareup.com/apps
# Web Payments SDK (browser)
NEXT_PUBLIC_SQUARE_APPLICATION_ID=
NEXT_PUBLIC_SQUARE_LOCATION_ID=

# Optional: server-only location id for catalog/inventory (defaults to NEXT_PUBLIC_SQUARE_LOCATION_ID)
# SQUARE_LOCATION_ID=

# Catalog + Payments API (server only — never commit real tokens)
# Use a Sandbox access token from Developer Dashboard → Credentials → Sandbox;
# production tokens only work with SQUARE_ENVIRONMENT=production.
SQUARE_ACCESS_TOKEN=
# sandbox (default) | production — must match the token above
SQUARE_ENVIRONMENT=sandbox

# Optional: comma-separated Square catalog variation IDs for homepage “Fan favorites”
# NEXT_PUBLIC_FEATURED_CATALOG_IDS=

# Set to 1 to print Square catalog + inventory API responses in the server log (off in production unless set)
# SQUARE_DEBUG=1
